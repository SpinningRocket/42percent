class TimeBoardData {

  String title;
  String unit;
  int maxValue;
  int currentValue;
  bool initializeAnimation;

  TimeBoardData(this.title, this.unit, this.maxValue, this.currentValue, this.initializeAnimation);

}